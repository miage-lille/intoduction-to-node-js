/*
import { persistence } from '../src/node/ex7/server';

describe(`function persistence takes in a positive parameter num
and returns its multiplicative persistence, which is the number of times you must multiply
the digits in num until you reach a single digit.`, () => {
  test('because 3*9 = 27, 2*7 = 14, 1*4=4 and 4 has only one digit', () => {
    expect(persistence(39)).toBe(3);
  });

  test('because 9*9*9 = 729, 7*2*9 = 126, 1*2*6 = 12, and finally 1*2 = 2', () => {
    expect(persistence(999)).toBe(4);
  });

  test('because 1*0*6*9 = 0', () => {
    expect(persistence(1069)).toBe(1);
  });

  test('because 4 is already a one-digit number', () => {
    expect(persistence(4)).toBe(0);
  });
});
*/
